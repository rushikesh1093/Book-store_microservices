# Application
APP_ENV=development
APP_DEBUG=True

# Database (PostgreSQL via AWS RDS)
DATABASE_URL=

# Django backend base URL (for cross-service calls)
DJANGO_API_URL=http://localhost:8000

# CORS
CORS_ORIGINS=http://localhost:3000
