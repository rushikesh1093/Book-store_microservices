name: Microservice Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    name: Deploy to Railway
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # TODO: Add Railway CLI deploy step
      # TODO: Add RAILWAY_TOKEN secret in GitHub repository settings
      - name: Placeholder deploy step
        run: echo "TODO - wire up Railway deployment token and CLI"
